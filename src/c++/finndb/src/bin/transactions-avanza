#!/usr/bin/env bash

tail +2 | \
    ../build/install/bin/shafy | \
    iconv -f LATIN1 -t UTF8 | \
    sed -e 's/,//g' | \
    awk -F ';' '{ print ("insert into movements (id, date, asset, account, amount, decimals) values (\"" $11 "\",\""$1 "\",\"" $9 "\",\"" $2 "\"," $7 ",2);") }'
